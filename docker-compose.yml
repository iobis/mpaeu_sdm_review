version: "3.9"

services:
  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_DB: sdmr
  #     POSTGRES_USER: sdmr
  #     POSTGRES_PASSWORD: sdmr
  #   volumes:
  #     - ./data/postgres:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:3000
    volumes:
      - .:/app
      - ./data/media:/app/media
    ports:
      - "3000:3000"
    # environment:
    #   POSTGRES_DB: sdmr
    #   POSTGRES_USER: sdmr
    #   POSTGRES_PASSWORD: sdmr
    #   DB_HOST: db
    #   DB_PORT: 5432
    # depends_on:
    #   - db